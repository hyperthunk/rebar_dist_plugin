
{sub_dirs, ["rel"]}.

{deps, [
    {rebar_dist_plugin, ".*",
        {git, "../../../../"}}
]}.

{plugins, [rebar_dist_plugin]}.
{plugin_dir, "../../src"}.

{dist, [
    {format, zip},
    {assembly, project}
]}.
