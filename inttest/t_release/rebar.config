
{sub_dirs, ["rel"]}.

{deps, [
    {rebar_dist_plugin, ".*",
        {git, "../../../../../", "master"}}
]}.

{rebar_plugins, [rebar_dist_plugin]}.
{plugin_dir, "../../src"}.

{dist, [
    {attach, [clean]},
    {assembly, release}
]}.
