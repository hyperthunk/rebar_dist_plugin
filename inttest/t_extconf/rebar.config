
{deps, [
    {'rebar-dist-plugin', ".*",
        {git, "../../../../../", "master"}}
]}.

{rebar_plugins, [rebar_foo_plugin, rebar_dist_plugin]}.
{plugin_dir, "../../src"}.

{dist, [
    {config, "dist.config"}, 
    {assembly, "foo"}
]}.
